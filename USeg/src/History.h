#pragma once

namespace umath
{
	class History
	{
		// selections
		// operations
		// results
		// current state

		/* How to reference elements? Maybe use tree directly,
		 * so if a Focal branches and you want branch B, you must access through the original Focal?
		 * All the results are up to date, so it is just tree traversal, not re-calculation.
		 */
	};
}
