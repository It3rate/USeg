#pragma once

namespace umath
{
	class Operation
	{
		// Operations on selection(s)
		// Add (jump, shift)
		// Multiply (stretch)
		// Repeat
		// Turn
		// Twist (3d)
		// Branch/Join (connections are 0, 1 or 2 way links)
		// Duplicate

		// Compare
		// Choose
	};

}